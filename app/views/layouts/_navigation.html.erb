<nav class="navbar navbar-default" style="<%= 'background-color:#f4f4f4' if logged_in? %>">
  <div class="container-fluid">
    
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      
      <div class="navbar-image-house">
        <%= link_to root_path do %>
          <% if logged_in? %>
            Dip
          <% elsif current_page?(:controller => 'pages', :action => 'home') %>
            <div class="logo-black-bg">
              <%= image_set_tag_3x("logo_white.png") %>
            </div>
          <% elsif current_page?(:controller => 'users', :action => 'new') || 
              current_page?(:controller => 'sessions', :action => 'new') %>
              <%= image_set_tag_3x("logo_black.png") %>
          <% end %>
        <% end %>
      </div>
      
    </div>
    
    <% if current_page?(:controller => 'users', :action => 'new') || 
            current_page?(:controller => 'sessions', :action => 'new') %>
      
      <div class="nav navbar-nav navbar-right">
        <div class="navbar-image-house">
          <%= link_to root_path do %>
            <%= image_set_tag_3x("cancel.png") %>
          <% end %>
        </div>
      </div>
    
    <% else %>
      
      <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      
      <!--
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
      -->
      
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li style="margin-top:15px"><%= link_to "Upload", new_dip_path %></li>
          <li style="margin-top:15px"><%= link_to "Profile", user_path(current_user) %></li>
          <li style="margin-top:15px"><%= link_to "Log out", logout_path, method: :delete %></li>
        <% else %>
          <li>
          <%= link_to signup_path do %>
            <button id="nav-sign-up" class="nav-sign-up">Sign up</button>
          <% end %>
          </li>
          <li>
          <%= link_to login_path do %>
            <button id="nav-log-in" class="nav-log-in">Log in</button>
          <% end %>
          </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
    
    <% end %>
  </div><!-- /.container-fluid -->
</nav>